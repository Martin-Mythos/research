import React from 'react';import{createRoot}from'react-dom/client';import{siteContent as c}from'./content';import'./styles.css';
function App(){const cats=[...new Set(c.menu.map(i=>i.category))];return <><header className="hero"><div><p className="eyebrow">Local takeaway · Tervuren</p><h1>{c.shop}</h1><p className="lead">{c.positioning}</p><p className="notice">{c.notice}</p><nav className="actions"><a href={`tel:${c.contact.phone}`}>Call</a><a href={c.contact.mapsUrl}>Open Google Maps</a><a href="#menu">View menu</a></nav></div></header><main><section id="menu"><h2>Menu highlights</h2><p>Sample prices and placeholder photos until the owner confirms final menu/assets.</p>{cats.map(cat=><div key={cat}><h3>{cat}</h3><div className="cards">{c.menu.filter(i=>i.category===cat).map(i=><article className="card" key={i.name}><div className="photo">Photo placeholder</div><h4>{i.name}</h4><p>{i.price}</p><p>{i.allergens.map(a=><span className="tag" key={a}>{a}</span>)}{i.allergens.length===0&&<span className="tag">ask staff</span>}</p></article>)}</div></div>)}</section><section><h2>Seasonal promotions</h2>{c.promotions.map(p=><article className="promo" key={p.title}><strong>{p.title}</strong><span>{p.time}</span><p>{p.text}</p></article>)}</section><section><h2>Allergy information</h2><p>Visible tags are a planning placeholder. Full allergen details are available in-store or by request before ordering.</p><div>{c.allergens.map(a=><span className="tag" key={a}>{a}</span>)}</div></section><section><h2>Opening hours</h2>{c.hours.map(h=><article key={h.season}><h3>{h.season}</h3><ul>{h.rows.map(r=><li key={r}>{r}</li>)}</ul></article>)}</section><section><h2>Visit / contact</h2><div className="contact"><a href={c.contact.mapsUrl}>Google Maps</a><a href={`tel:${c.contact.phone}`}>{c.contact.phone}</a><a href={c.contact.instagramUrl}>Instagram</a><span>{c.contact.email}</span></div></section></main><footer>Static-hosting friendly. Future domain option: talkingbreads.be.</footer></>}
createRoot(document.getElementById('root')!).render(<App/>);
